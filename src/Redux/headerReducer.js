
let initialState = {
    logoUrl: "https://raw.githubusercontent.com/Glendi123/Glendi123.github.io/master/img/logo.png",
}

const headerReducer = (state = initialState , action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default headerReducer;